rule meta:
    input:
        rscript = "meta.r",
        xlsx = "Final table mice (1).xlsx",
    output:
        tsv = "meta.tsv"
    shell:
        "Rscript {input.rscript} --infile '{input.xlsx}' --outfile {output.tsv}"
